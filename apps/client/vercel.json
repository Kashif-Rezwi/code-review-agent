{
    "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
    "buildCommand": "cd ../.. && pnpm --filter client build",
    "ignoreCommand": "bash -c 'if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then git diff --quiet ${VERCEL_GIT_PREVIOUS_SHA:-HEAD^1} $VERCEL_GIT_COMMIT_SHA -- ./ ../../packages/ ../../package.json ../../pnpm-lock.yaml ../../pnpm-workspace.yaml; else exit 1; fi'"
}